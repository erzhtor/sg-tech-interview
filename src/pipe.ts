/**
 * A pipe function which chains multiple (unary, i.e. takes one argument) functions together to create a new function.
 */
export function pipe(funcs: ((arg: any) => any)[]): (arg: any) => any {
    // your code here

    return () => {};
}
